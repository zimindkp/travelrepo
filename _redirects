# _redirects file

# Rule 1: Allow access to the login page itself (must be first!)
/login.html /login.html 200

# Rule 2: TEMPORARY - Redirect ALL other content to login.html
# This rule has NO role condition, so it should apply to everyone.
/* /login.html 302!